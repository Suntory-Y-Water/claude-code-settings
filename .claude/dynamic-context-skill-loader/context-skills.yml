# yaml-language-server: $schema=./context-skills-schema.json
skills:
  - name: managing-git-github-workflow
    description: Git operation automation and GitHub CLI wrapper
    trigger:
      required: "git"
      any:
        [
          "add",
          "commit",
          "push",
          "pull",
          "branch",
          "repo",
          "repository",
          "issue",
        ]
