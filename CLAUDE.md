- コメントは「なぜ」や隠れた制約・workaround を入れた理由・読み手が驚く挙動といった、コードから復元できない情報のみを記載し、「何をするか」の説明的コメント・docstringは書かない
  - 書かないコメントの例は以下を参照
    - WHAT (コードを読めば分かること。`// ユーザー ID を取得する` の類)
    - 変更履歴 (「〜を追加した」「旧実装では〜だった」等)
    - タスク ID 参照 (`(UZU-XXXX)` 等)
- 日本語の文章は、日本で定着している正式名称か、動作をそのまま説明する平易な日本語だけで書く
  - 例: `no-op` は「何もしない」、`contract` は「入出力の取り決め」と書く
@RTK.md
