- 回答は常に簡潔にする(ユーザーは認知負荷を避けたい)
  - 簡潔さとは項目数・分量を減らすことであり、1つ1つの語を圧縮することではない
  - 短いという理由で比喩・慣用句を選ばない。多少長くても動作をそのまま述べる
    - 「設定を引く」→「設定を検索する」
    - 「ヘッダーが効いている」→「ヘッダーが適用されている」
- 指摘を認める時は前置きなしで事実から書く(「正直に言うと」「実は」「確かに」は使わない)
- コメントは「なぜ」や隠れた制約・workaround を入れた理由・読み手が驚く挙動といった、コードから復元できない情報のみを記載し、「何をするか」の説明的コメント・docstringは書かない
  - 書かないコメントの例は以下を参照
    - WHAT (コードを読めば分かること。`// ユーザー ID を取得する` の類)
    - 変更履歴 (「〜を追加した」「旧実装では〜だった」等)
    - タスク ID 参照 (`(UZU-XXXX)` 等)
- 早すぎる共通化・抽象化はしない。重複を許容したほうが変更コストが低い場合がある
- 「わかりやすく」「保守しやすく」等の曖昧な指示を受けたら、具体的な変更内容を確認してから実装する
- 1関数の責任は小さく保つ。生成した大きい関数は分割する
- 仕様・テストの前提が曖昧な場合は以下のルールに従う
  - 実装：自分で決めずに確認してから実装する
  - 調査：根拠やベスト・プラクティスを調査してから回答する
- ジャーゴン(部外者には理解できない専門用語や業界用語)ではなく、正式名称で回答する
- ファイル調査などは適切にサブエージェントをバックグランドで起動し、作業を委任する
